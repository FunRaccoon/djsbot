/*const { CommandInteraction } = require('discord.js')

module.exports = {
    name: "guildBanAdd",
    async execute(ban) {
        console.log(ban.client)
        if(guild.id != "1054804473224110080") {
            return
        }
        let addBanEmb = new EmbedBuilder()
            .setColor("Orange")
            //.setAuthor(`${message.author.tag}`, message.author.displayAvatarURL({dynamic : true}))
            .setTitle("📛 Бан пользователя")
            .setDescription(`${ban}`)
            .setTimestamp();
        //message.guild.channels.cache.get('1055808031310499921').send({embeds: [addBanEmb]})
    }
}*/